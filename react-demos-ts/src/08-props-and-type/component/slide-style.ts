const headingText = {
    fontSize : '4em'
};

const bodyText = {
    fontSize : '2.5em'
};

const textCenter = {
    textAlign : 'center' as const
};

const listUnStyled = {
    listStyleType : 'none',
    padding : 0,
    margin : 0
};
const paragraphStyle = {
    fontWeight : 'bold',
}
const headingStyle = {
    color : 'green'
}
export {
    paragraphStyle,
    headingStyle,
    headingText,
    bodyText,
    textCenter, 
    listUnStyled
}